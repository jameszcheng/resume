# Resume - James Cheng

LaTeX resume with automated PDF builds via GitHub Actions.

## Usage

Edit `resume.tex` and push to `main`. The workflow automatically compiles the PDF and uploads it as a release artifact.

## Local Build

```bash
pdflatex resume.tex
```

## Download

The latest compiled PDF is available under [Releases](../../releases/tag/latest).
